/**
 * @deprecated The Studios / Performers filter was merged into the curved Home
 * wall — it now lives as the left "filter rail" inside [VRHomePanel]. This module
 * is retained only as a type alias for `IVRFilterEntry`; import it from
 * "./VRHomePanel" directly in new code.
 */
export type { IVRFilterEntry } from "./VRHomePanel";
