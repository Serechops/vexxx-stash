type Version {
  version: String
  hash: String!
  build_time: String!
  repo: String!
}

type LatestVersion {
  version: String!
  shorthash: String!
  release_date: String!
  url: String!
  repo: String!
}
