# Example environment variables for docker-compose
# Adjust these to match your host paths and preferences.

# Local host path that will be mounted into the container as /root/.stash
HOST_CONFIG=./stash_config

# Local host path (or a single path) to mount media libraries into /media
# You can set this to an absolute path on Windows: C:\\path\\to\\media
HOST_MEDIA_PATH=./media

# Optional host folder for generated assets (thumbnails, transcodes, etc.)
HOST_STORAGE=./stash_generated

# Container port mapping
HOST_PORT=9999

# Build args / metadata
GITHASH=dev
STASH_VERSION=dev

# UID / GID for file ownership within the container (useful on Linux)
PUID=1000
PGID=1000

# Timezone
TZ=UTC
